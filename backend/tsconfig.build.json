{
  // Production build: compiles only src/ into dist/ (tests are not shipped).
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
